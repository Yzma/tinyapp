
/**
 * The Users Object that will store all users.
 * Note that due to the simplicity of the app, we just export an empty Object for now, but later on
 * if we use a Database (such as SQL) it would be required to move the Users Object into a separate file.
 */
const users = {}

module.exports = users
